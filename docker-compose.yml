services:
  server:
    build: .
    container_name: challenges
    volumes:
      - .:/opt/backend/
    environment:
      MONGO_URI: mongodb://localhost:27017/challenges-dev
      WORKER_URI: http://localhost:9001
    network_mode: "host"